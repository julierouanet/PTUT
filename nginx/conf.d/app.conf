# ────────────────────────────────────────────────────────────
# app.lucaslopvet.fr  →  Flutter web (production)
# ────────────────────────────────────────────────────────────
server {
    listen 80;
    server_name app.lucaslopvet.fr;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    server_name app.lucaslopvet.fr;

    ssl_certificate     /etc/letsencrypt/live/app.lucaslopvet.fr/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/app.lucaslopvet.fr/privkey.pem;
    include             /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam         /etc/letsencrypt/ssl-dhparams.pem;

    root  /var/www/flutter-app;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # Fichiers Flutter à noms FIXES : revalidation seulement (no-cache, jamais
    # no-store) sinon un déploiement n'est jamais détecté par le navigateur.
    location ~* ^/(main\.dart\.js|flutter\.js|flutter_bootstrap\.js|flutter_service_worker\.js)$ {
        add_header Cache-Control "no-cache";
    }

    # Assets réellement immuables (CanvasKit, polices/manifests Flutter) : cache 1 an.
    # Doit être avant la règle générique ci-dessous pour ne pas hériter du cache long
    # sur des fichiers à noms fixes (main.dart.js, etc.).
    location ~* ^/(canvaskit|assets)/ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # Cache assets Flutter (autres fichiers hashés/statiques)
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff2?)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    gzip on;
    gzip_comp_level 5;
    gzip_min_length 1024;
    gzip_types text/plain text/css application/javascript application/wasm application/json image/svg+xml;
}
