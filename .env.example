# Copier ce fichier en .env sur le VPS et remplir les valeurs
JWT_SECRET=changez-moi-en-production-une-vraie-cle-secrete
JWT_REFRESH_SECRET=changez-moi-en-production-une-vraie-cle-refresh

# Dev (optionnel, des valeurs par défaut existent dans docker-compose.dev.yml)
JWT_SECRET_DEV=kabutare-dev-secret
JWT_REFRESH_SECRET_DEV=kabutare-dev-refresh-secret
